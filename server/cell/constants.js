const LEFT = 'left'
const RIGHT = 'right'
const UP = 'up'
const DOWN = 'down'

const CELL_TYPES = {
  CELL: 'CELL',
  EMPTY: 'EMPTY',
  OUT_OF_BOUNDS: 'OUT_OF_BOUNDS',
  SHIP: 'SHIP',

}

module.exports = { DIRECTIONS: { RIGHT, DOWN, LEFT, UP }, CELL_TYPES }
